"""Stable task-level protocol exposed to the desktop application.

Track A owns the sidecar boundary. The implementation must follow the schemas
in ``contracts/`` and must not expose arbitrary shell execution.
"""
